# 设计规范：Clone Studio（暂定名）

> v1.0 · 2026-09-19 · 对应 Product-Spec v1.1

## 0. AI 使用说明

- 本文档定义产品的界面结构、视觉系统、组件、状态和交互规则。
- Product Spec 是功能范围的事实来源；本文档不得新增 Product Spec 没有定义的核心功能。
- 本产品是叠加形态：界面型为主体（§2 §3 §5 §8.3），内嵌一个只读的 Agent 过程抽屉（§A）。
- AI MUST 优先设计 P0 flows 和 P0 screens。
- AI MUST 覆盖默认、加载、空、错误、成功等关键状态。
- AI MUST 保持页面、组件、交互和视觉 tokens 的一致性。
- 只出深色主题一套。只出桌面端（≥1280px）。

---

## 1. 设计方向

### 1.0 产品形态

| 维度 | 方向 |
|---|---|
| 形态 | 界面型 + 嵌入式对话式 Agent 视图（叠加） |
| 本体 | 主体是页面与组件；Agent 抽屉的本体是会话流与渲染，v1 只读 + 中止，不可对话 |
| 落地分轨 | 主体用 §2 §3 §5 §8.3；抽屉用 §A |

### 1.1 设计目标

让操作员一眼扫完一个模板下 20 条变体各自卡在哪、花了多少、哪几条在等他动手，并且视频和素材图永远是画面里最亮的东西。

### 1.2 产品气质

| 维度 | 方向 |
|---|---|
| 语气 | 专业、直接、不寒暄 |
| 信息密度 | 紧凑 |
| 视觉风格 | 深色专业工具型（Linear 的精确 + 视频工具的暗房底色） |
| 情绪目标 | 一切在掌控中：钱没乱花、Agent 在干什么看得见、下一步该我做什么很明确 |

### 1.3 设计原则

- DP-001 媒体最亮：界面用中性深灰，不给容器上色；视频、缩略图、素材图是唯一的高亮度大色块。
- DP-002 状态一眼可辨：每条任务的状态用"色点 + 文字"双编码，永不只靠颜色；需要人动手的两种状态（素材待审、待确认花费）在视觉上比其它状态高一级。
- DP-003 钱永远可见：估价、已花、限额出现在做决定的地方旁边，金额一律等宽数字右对齐。
- DP-004 过程不外溢：Agent 的过程性回显只在右侧抽屉里；工作区只放结果和需要人决定的东西。
- DP-005 强调色吝啬：电光青绿只给主操作、进行中、选中态。一屏内强调色面积不超过约 5%。
- DP-006 紧凑但不缩点击区：行高和间距收紧，按钮、勾选框、素材图上的操作热区不小于 28px。

### 1.4 参考与反参考

**正向参考：**

| 参考产品 | 具体喜欢哪一点（拆成属性） | 必须保留 |
|---|---|---|
| Linear | 紧凑行列表、1px 细边框、小圆角、克制的单一强调色、键盘友好、状态图标体系 | Yes |
| DaVinci Resolve / 剪映专业版 | 深灰暗房底色让视频最亮、页面级步骤切换 | Yes |
| Claude Code | 等宽会话流、工具调用折叠成一行、逐字流式、随时可中止 | Yes（仅抽屉） |
| Vercel 部署列表 | 每行一条任务：状态点 + 名称 + 耗时 + 触发信息，失败行可展开看错误原文 | No |

**反参考：**
- 剪映消费版 / Canva：大圆角、彩色图标、引导气泡多，占地方，天天用嫌啰嗦。
- 通用"AI 产品"紫色渐变 + 毛玻璃 + 发光描边：装饰压过内容，且让视频色彩判断失真。
- Runway 式大封面大留白：一屏只放 6-8 条，批量 20 条要滚动。
- Grafana 式满屏图表：本产品没有需要图表的数据，花费用数字就够。

---

## 2. 信息架构

### 2.1 导航结构

常驻左侧栏（客户 → 模板树）+ 主区。模板页内部是顶部横向五步流水线（①参考 ②复刻 ③验货 ④变体 ⑤成片），步骤条既是进度也是导航，已解锁的步骤可随时点回去看。右侧 Agent 抽屉可收起，收起后留一条 32px 窄轨显示运行中任务数。侧栏底部固定"设置"入口和环境体检状态点。

### 2.2 页面清单

| 页面编号 | 页面名称 | 页面目的 | 关联流程 / 需求 | 优先级 |
|---|---|---|---|---|
| SCREEN-001 | 应用外壳 + 侧栏 | 归档导航、全局状态 | FLOW-004 / REQ-001 | P0 |
| SCREEN-002 | 首页空状态 / 客户页 | 建第一个客户；客户下模板概览 | FLOW-004 / REQ-001 | P0 |
| SCREEN-003 | 模板 · ①参考 | 导入参考视频，看证据准备进度 | FLOW-002 / REQ-002、REQ-010 | P0 |
| SCREEN-004 | 模板 · ②复刻 | 看复刻结果摘要、check 结果、估价 | FLOW-002 / REQ-003、REQ-004 | P0 |
| SCREEN-005 | 模板 · ③验货 | 原片与复刻片并排对比，通过或打回 | FLOW-002 / REQ-004、REQ-006 | P0 |
| SCREEN-006 | 模板 · ④变体 | 提交批量 brief，盯队列 | FLOW-003 / REQ-005、REQ-006、REQ-010 | P0 |
| SCREEN-007 | 变体详情 · 素材审核（右侧大抽屉或全幅面板） | 审图、替换、看台词、确认花费 | FLOW-003 / REQ-005、REQ-006、REQ-009 | P0 |
| SCREEN-008 | 模板 · ⑤成片 | 播放、下载、批量下载 | FLOW-003 / REQ-007、REQ-009 | P0 |
| SCREEN-009 | 设置 | 环境体检、生成服务凭据、模型档案、限额与并发 | FLOW-001 / REQ-008、REQ-010 | P0 |

### 2.3 页面关系

主路径：SCREEN-002 → 新建模板 → 003 → 004 → 005 →（通过）→ 006 ⇄ 007 → 008。003-008 共用同一模板页框架，只换步骤条下方的工作区。009 从侧栏底部进入；任一体检 P0 项未过时，主区顶部出现一条琥珀色横幅直达 009。Agent 抽屉跟随当前所选对象：在 003-005 显示复刻任务，在 007 显示该变体的任务。

---

## 3. 页面规格

### SCREEN-001: 应用外壳 + 侧栏

**布局结构：**
1. 左侧栏 240px：顶部产品名；客户列表（可折叠树，模板为子项，每个模板前一个状态点）；悬停行尾出现"…"菜单（重命名、删除）；客户列表底部"+ 新客户"。侧栏最底：设置入口 + 体检状态点。
2. 主区：自适应。
3. 右侧 Agent 抽屉：默认 420px，可拖宽到 640px，可收起。

**必需状态：** 加载=树骨架 6 行；空=侧栏只有"+ 新客户"；错误=后端未响应时侧栏顶部红色细条 + 重试；删除确认=弹窗，需输入名称，列出将中止的任务数。

### SCREEN-002: 首页空状态 / 客户页

**主要操作：** 新建客户（空状态）/ 新建模板（客户页）。

**布局：** 空状态居中一句话 + 一个主按钮，不放插画。客户页是模板的紧凑行列表：状态点、模板名、参考视频缩略帧（小）、成片数、累计花费、最近活动时间。

### SCREEN-003: 模板 · ①参考

**布局结构：**
1. 顶部：模板名（可就地改）、模板累计花费、步骤条 CMP-001。
2. 主内容区左：导入表单——上传区与链接输入二选一的分段控件、语言下拉、复刻备注、Agent 模型下拉 CMP-010、提交按钮。
3. 主内容区右：提交后变为参考视频播放器 + 证据准备清单（下载/探测/转写/抽帧，每项状态图标 + 耗时）。

**必需状态：** 默认=表单；加载=清单逐项转圈→打勾；错误=失败项变红、展开错误原文（等宽）、"重试此步"；成功=自动跳 ②；禁用=所选模型档案不支持看图时提交按钮禁用，下方一行说明。

### SCREEN-004: 模板 · ②复刻

**布局：** 左 60% 三个可折叠区块：分析摘要（ANALYSIS.md 渲染）、时间线（TIMELINE.md 渲染成带时间码的列表，点时间码右侧参考视频跳到该处）、校验结果（check 通过/失败 + 原文）。右 40%：参考视频小播放器 + 估价卡 CMP-006。

**必需状态：** 加载=Agent 运行中，工作区显示"复刻进行中"+ 已用时长/等价花费/所用模型，三个区块在对应文件产生后逐个出现；错误=已熔断横条 CMP-009（继续 / 重跑）；等待额度=蓝灰横条显示预计恢复时间；成功=估价卡出现，限额内显示"将自动出片"，超限显示主按钮"确认出片 $x.xx"。

### SCREEN-005: 模板 · ③验货

**主要操作：** 通过 / 打回。

**布局结构：**
1. 主内容区：并排播放器 CMP-004，左"原片"右"复刻片 vN"，9:16 竖屏按高度适配，两片等高。下方一条共享进度条 + 播放/暂停、倍速、声音来源切换（原片/复刻片）。
2. 播放器下方一行差异信息：时长差、分辨率。版本切换用分段控件 v1 / v2 / …。
3. 底部固定操作区：左"打回"（次要按钮，点开后展开意见输入框）；右"通过验货"（主按钮）。

**必需状态：** 加载=渲染中，右侧播放器位置显示进度 CMP-007；错误=出片失败卡，错误 code + message 原文 + "重试出片"；成功=通过后步骤条 ④ 解锁并自动跳转；禁用=复刻片未出时两个按钮禁用。

### SCREEN-006: 模板 · ④变体

**布局结构：**
1. 上部提交区（可折叠）：多行 brief 文本框（等宽行号，实时显示"N 条"）、目标语言、批次备注、Agent 模型下拉、批次限额与已花 CMP-006 迷你版、提交按钮。空状态时文本框下方列 3 条示例 brief，点击填入。
2. 下部队列：变体紧凑行列表 CMP-002，按批次分组，组头显示批次花费条。每行：状态 CMP-003、名称/brief 首句、所用模型、Agent 耗时、估价/实际花费、行尾动作。需要人动手的行（素材待审、待确认花费）置顶并带左侧 2px 强调色/琥珀色竖线。
3. 列表上方筛选：全部 / 需要我处理 / 进行中 / 完成 / 失败。

**必需状态：** 禁用=模板未验货时整页锁定，显示"先通过验货"；错误=超 20 条、单行超 500 字在文本框下红字，不提交；批次达限额=组头琥珀色提示"批次限额已用尽，剩余变体等你确认"。

### SCREEN-007: 变体详情 · 素材审核

**布局：** 从队列行点开，覆盖工作区的全幅面板（保留步骤条和侧栏）。左 65% 素材网格 CMP-005；右 35% 上为台词全文（等宽、按段），下为估价卡与动作区。

**主要操作：** 素材通过。**次要操作：** 替换单张、打回写意见、取消该变体。

**必需状态：** 无来源 URL 的图卡片角标黄色"无来源"；Agent 标注的缺口卡片红色虚线占位 + "上传"；底部一行小字"联网图片版权由使用者自行负责"；通过后若超限，动作区变为"确认出片 $x.xx"。

### SCREEN-008: 模板 · ⑤成片

**布局：** 紧凑网格（竖屏封面，每行 6-8 个，1280px 下 6 个），卡片下方：名称、时长、花费、状态。点开为居中播放器弹层，右侧列花费明细 CMP-008。顶部工具条：多选、批量下载 zip、筛选。

**必需状态：** 空=一句话 + 去 ④ 的链接；加载=封面骨架；错误=失败卡片红色状态 + 点开看错误原文与重试。

### SCREEN-009: 设置

**布局：** 单列分区长页，左侧页内锚点：环境体检 / 生成服务 / Agent 模型 / 限额与熔断 / 并发 / 路径。
- 环境体检：每项一行 CMP-011（状态图标、名称、现状、修复命令可复制）。"重新检测""一键准备"。
- 生成服务：TokenDance key 输入（打码 + 验证按钮 + 已验证时间）；HypiHub 一行"连接"按钮（浏览器授权）与连接状态。
- Agent 模型：档案列表 CMP-010 管理视图——名称、类型、模型 id、看图/搜索能力徽标、已验证时间、默认标记；行内"测试连接""设为默认""删除"；"+ 添加模型"打开右侧表单面板，先选预设。预设旁固定一行说明"需要 API key，聊天订阅不可用"。
- 限额与熔断、并发：数字输入 + 单位，保存即生效。

**可访问性说明（全局）：** 全部操作可键盘到达；焦点环 2px 强调色；状态不只靠颜色。

---

## 4. 组件规格

| 编号 | 组件 | 用途与要点 | 使用位置 |
|---|---|---|---|
| CMP-001 | 流水线步骤条 | 五步横排，圆点+序号+名称。态：未解锁（灰、锁图标、不可点）/ 可进入 / 当前（强调色下划线）/ 进行中（圆点脉动）/ 需处理（琥珀点）/ 完成（对勾）/ 失败（红点） | 003-008 |
| CMP-002 | 任务紧凑行 | 行高 36px，列对齐，数字列等宽右对齐；悬停显行尾动作；失败行可就地展开错误原文 | 002、006 |
| CMP-003 | 状态标记 | 色点 8px + 文字。排队=灰；Agent 写稿/渲染中=强调色脉动；等待额度=蓝灰；素材待审=强调色实心+"待你审"；待确认花费=琥珀；完成=绿对勾；失败/已熔断=红；中断=灰斜纹；已取消=灰删除线 | 全局 |
| CMP-004 | 并排同步播放器 | 两路视频共享一条进度条与控制；声音来源切换；逐帧前后键；任一路缓冲时两路一起暂停 | 005 |
| CMP-005 | 素材审核卡 | 缩略图 + 条目名 + 来源域名链接；悬停出"替换""看大图"；角标：无来源（黄）、已替换（强调色）、缺口（红虚线） | 007 |
| CMP-006 | 估价/限额卡 | 估价金额大号等宽；下方请求明细折叠列表；单条限额与批次限额各一条细进度条，超限部分琥珀；底部说明"将自动出片"或主按钮"确认出片 $x.xx" | 004、006、007 |
| CMP-007 | 出片进度 | 阶段文字（提交/生成素材 n/m/渲染/保存）+ 细进度条 + 已用时长；"取消" | 005、006 |
| CMP-008 | 花费明细 | 两列表：Agent 任务（模型、时长、等价花费）与 Build（估价、实际、build-id）；估算值带"估"徽标；合计 | 007、008 |
| CMP-009 | 熔断/中断横条 | 红或灰横条：原因（超时/超预算/卡死/中断）+ 已用时长与花费 + "继续""重跑"；重跑可重选模型 | 004、007 |
| CMP-010 | 模型档案选择器 | 下拉：档案名 + 模型 id 小字 + 能力徽标（看图/搜索）；不满足当前任务要求的项置灰并附原因；底部"管理模型…"跳设置 | 003、006、009 |
| CMP-011 | 体检行 | 图标（过/未过/检测中）+ 名称 + 现状 + 修复命令（等宽，可复制） | 009 |
| CMP-012 | 危险确认弹窗 | 列出级联影响与将中止的任务数；需输入名称；确认按钮红色 | 全局 |

组件通用七态：默认 / 悬停（表面提亮一级）/ 聚焦（2px 强调色环）/ 激活 / 禁用（40% 不透明度 + 原因 tooltip）/ 加载（骨架或行内转圈，不整页遮罩）/ 错误（红边 + 下方原文）。

---

## 5. 视觉系统

### 5.1 色彩变量（深色单主题，具体值交设计工具微调）

| 变量 | 用途 | 值 / 方向 |
|---|---|---|
| color.background | 应用底 | 近黑中性灰，约 #0E0F11，不带蓝紫偏色 |
| color.surface | 侧栏、卡片、面板 | 约 #16181B；悬停/抬升一级约 #1D2024 |
| color.border | 分割线、边框 | 1px，约 #2A2E33 |
| color.primary | 主操作、进行中、选中 | 电光青绿，约 #19E3B1；其上文字用近黑 |
| color.warning | 待确认花费、超限、无来源 | 琥珀，约 #F5A524 |
| color.danger | 失败、熔断、删除 | 红，约 #F0495A |
| color.success | 完成、体检通过 | 绿，约 #3FCF6B，须与 primary 靠"对勾图标"区分而非仅靠色相 |
| color.info | 等待额度 | 蓝灰，约 #7C8AA5 |
| color.text.primary / secondary / tertiary | 文本三级 | 约 #ECEEF1 / #9BA1AA / #666C75 |

### 5.2 字体排版

| 变量 | 用途 | 规格方向 |
|---|---|---|
| 字族 | 界面 | Geist + 苹方/微软雅黑等系统中文黑体回退（设计稿采用，取代初稿的 Inter） |
| 字族 | 数字、金额、时间码、模型 id、命令、Agent 抽屉 | JetBrains Mono 或同类等宽，开启 tabular-nums |
| text.heading.lg | 模板名 | 18/600/1.3 |
| text.heading.md | 区块标题 | 14/600/1.4 |
| text.body | 正文、列表行 | 13/400/1.5 |
| text.caption | 辅助、徽标 | 12/400/1.4 |

### 5.3 间距系统

4px 步进：xs 4 / sm 8 / md 12 / lg 16 / xl 24。列表行高 36px，表单控件高 32px，最小点击区 28px。

### 5.4 形状与层级

radius.sm 4 / md 6 / lg 8，不用 pill。层级靠边框和表面亮度，不靠阴影；仅弹窗与下拉用一层柔和阴影。禁用渐变、毛玻璃、发光。

### 5.5 图标与图像风格

线性图标 1.5px 描边（Lucide 一类），单色跟随文本色。不用插画、不用 emoji。空状态只用文字和一个按钮。视频与图片不加滤镜、不加圆角大于 6px、不裁切比例（竖屏按 9:16 留黑边而非拉伸）。

---

## 6. 交互与反馈规则

### 6.1 全局交互规则

| 模式 | 规则 |
|---|---|
| 主操作 | 每个工作区至多一个强调色主按钮，放右下固定操作区或卡片底部 |
| 次要操作 | 描边或幽灵按钮；行内动作悬停才出现 |
| 危险操作 | 删除走 CMP-012 输入名称确认；取消任务、重跑走普通二次确认 |
| 花钱操作 | 按钮文案必带金额："确认出片 $0.92"；限额内自动放行时用一行文字说明，不弹窗 |
| 保存行为 | 设置项改完即存，行尾短暂出现"已保存"；重命名回车即存 |
| 导航 | 步骤条可点回已解锁步骤；刷新保持在当前模板与步骤 |
| 弹窗 / 抽屉 | 弹窗只用于危险确认和成片播放；添加模型用右侧表单面板；Agent 过程只在右抽屉 |

### 6.2 反馈规则

| 场景 | UI 反馈 | 文案规则 |
|---|---|---|
| 成功 | 状态标记变化 + 行短暂高亮，不弹 toast 打断；批量全部结束时一条 toast | 陈述事实："3 条完成，1 条失败" |
| 错误 | 就地红色 + 可展开的上游错误原文（等宽） | 先说哪一步失败，再给原文，再给能做的动作；不说"出错了请稍后再试" |
| 警告 | 琥珀色横条或竖线 | 说清卡在什么、要用户做什么 |
| 空状态 | 一句话 + 一个动作 | 不鼓励、不卖萌 |
| 加载 | 骨架屏/行内转圈；长任务显示阶段与已用时长 | 写正在做的事："转写中 · 0:42" |

### 6.3 动效规则

- 功能性、克制。过渡 120-160ms ease-out。
- 允许：进行中状态点脉动、抽屉开合、行展开、流式文字。
- 不用：页面切换动画、数字滚动、庆祝动效、骨架闪光以外的装饰循环。
- 尊重 prefers-reduced-motion：脉动改为静态。

---

## 7. UX 文案与内容规则

### 7.1 文案语气
清晰、直接、工具口吻。用动词和数字，不用感叹号，不拟人。

### 7.2 按钮文案规则
- 主按钮 = 动词 + 对象：开始复刻、提交 5 条变体、素材通过、通过验货、确认出片 $0.92。
- 次按钮：打回、重试出片、继续、重跑、取消。
- 危险按钮写明后果：删除客户及 12 条成片。

### 7.3 表单文案规则
- label 用名词；placeholder 给真实例子（"换成 2026 年手机品牌排行，毒舌风格"）；helper 写限制（"一行一条，最多 20 条"）；error 写哪里不对和怎么改。

### 7.4 空状态文案规则
- 说明这里将出现什么 + 下一步动作，一句话，不鼓励式。

---

## 8. 可访问性与响应式要求

### 8.1 可访问性目标
基础可访问性（Product Spec 未要求合规级别）。正文对比度按 WCAG AA 取值。

### 8.2 必需可访问性规则
- [ ] 所有可交互元素可通过键盘访问
- [ ] 焦点状态清晰可见
- [ ] 表单字段有明确 label
- [ ] 错误信息与对应字段关联
- [ ] 不只依赖颜色表达状态（状态=色点+文字，成功/主色靠图标区分）
- [ ] 正文和背景有足够对比度
- [ ] 图标有替代文本或标记为装饰性
- [ ] 页面标题和区块标题结构清晰

### 8.3 响应式规则

| 断点 / 设备 | 规则 |
|---|---|
| 移动端 / 平板 | 不支持，<1280px 显示"请在桌面浏览器使用" |
| 桌面 1280-1599 | 侧栏 240 + 主区 + 抽屉 420；抽屉展开时成片网格降为 4 列 |
| 桌面 ≥1600 | 抽屉可拖到 640；成片网格 8 列 |

---

## A. 对话式 Agent 形态规格（右侧过程抽屉）

### A.1 交互骨架与模式

| 维度 | 方向 |
|---|---|
| 驱动方式 | v1 只读。用户不能在抽屉里打字；给 Agent 的输入只经由工作区的"打回意见" |
| 模式 | 单一：后台自动执行。抽屉是观察窗 |
| 一轮交互 | 任务启动 → 流式过程 → 结束态（完成/熔断/中断/取消）。打回产生新一轮，接在同一会话流下方，用一条分隔线标"打回意见 #2" |

### A.2 呈现单元与技术内容

| 呈现单元 | 呈现方向 |
|---|---|
| 用户消息 | 仅系统代发的初始任务与打回意见，左侧细竖线 + 次级文字色 |
| Agent 回复 | markdown 渲染，等宽字体，逐字流式 |
| 工具调用 | 折叠成一行：图标 + 工具名 + 参数摘要 + 耗时 + 成败；点开看完整入参与输出 |
| 代码 / SVML | 语法高亮，>20 行默认折叠 |
| 命令输出 | >12 行折叠，保留首尾 |
| 思考 / 计划 / 待办 | 思考默认折叠为一行"思考 · 8s"；待办清单常驻抽屉顶部下方，勾选随进度更新 |
| 错误 / 堆栈 | 红色左竖线块；被宿主拦截的动作（如 build）用琥珀左竖线 + "已拦截：出片由宿主负责" |

### A.3 Agent 运行态

| 运行态 | 显示方向 |
|---|---|
| 流式输出中 | 末尾光标块 |
| 思考中 | 一行脉动文字 + 计时 |
| 跑工具中 | 工具行显示转圈与已用时长 |
| 等待授权 | 不适用（无交互授权；拦截即拒绝） |
| 等待额度 | 顶栏蓝灰条"额度受限 · 预计 14:20 恢复后自动继续" |
| 被中断 / 熔断 | 会话流末尾一条结束卡：原因 + 用时 + 花费；动作按钮在工作区 CMP-009，不在抽屉里 |
| 长任务进度 | 抽屉顶栏常驻：模型档案名与模型 id、已用时长、等价花费 / 熔断上限、"中止"按钮 |

### A.4 透明度与授权

| 维度 | 规则方向 |
|---|---|
| 推理展示 | 可展开，默认折叠 |
| 动作可回溯 | 全量落库，任务结束后仍可滚动回看；刷新后恢复 |
| 授权粒度 | 无逐次授权。Agent 拥有完整能力，两条硬拦截（花钱的 build、写工作目录之外）由宿主执行 |
| 动作分级 | 读写工作目录、联网、跑 hypit 工具直接做；build 与越界写一律拒绝 |
| 危险动作拦截 | 花钱只发生在工作区的估价卡：限额内自动、超限人点"确认出片 $x.xx" |
| 可中断 | 随时"中止"；不可在抽屉内改向，改向走"打回意见" |

### A.5 视觉与多 surface

| 维度 | 方向 |
|---|---|
| 渲染基元 | 网页内渲染，非真终端；等宽字体 13px，行高 1.55，无 box-drawing 装饰 |
| 色彩 | 沿用 §5 语义色；抽屉底色比 surface 再暗半级，与工作区区分 |
| 字体 | 全等宽 |
| 宽度 degrade | 420px 时工具行参数摘要截断加省略；≤360 不允许 |
| 多 surface | 仅网页。二期对话式精修时在抽屉底部加输入框，其余结构不变 |

---

## 9. 实现说明

- React + Vite + Tailwind + shadcn/ui（Radix）作组件底座，按本文 token 覆写主题；图标 Lucide。
- 只做深色主题，token 用 CSS 变量，为将来加浅色留口但 v1 不做。
- markdown 渲染 + 代码高亮用于抽屉与 ②复刻 的文档区块。
- 无品牌资产；产品名暂定，logo 用文字标。
- 避免：整页 loading 遮罩、toast 轰炸、用弹窗承载长流程、任何渐变/毛玻璃。

---

## 10. 假设与待确认问题

### 10.1 假设

| 编号 | 假设 | 假设依据 | 错误风险 |
|---|---|---|---|
| DASM-001 | 参考片与成片以 9:16 竖屏为主 | 用户做排行榜/混剪短视频 | 横屏片在并排播放器里偏小；播放器需按比例自适应，已要求不拉伸 |
| DASM-002 | 界面只做中文 | 用户全程中文沟通 | 低 |
| DASM-003 | 色值为方向性建议，设计工具可在同色相内微调 | Brief 只定方向 | 低 |
| DASM-004 | REQ-011（Codex 生图，P1）只在设置页多一条体检行和一个开关，不新增页面 | Spec 待文档定稿 | 文档到手后可能要补配置表单 |

### 10.2 待确认问题

| 编号 | 问题 | 是否阻塞 | 备注 |
|---|---|---:|---|
| DQ-001 | 产品正式名称与文字标 | No | 暂用 Clone Studio |
| DQ-002 | 素材审核用全幅面板还是右侧大抽屉 | No | 本文按全幅面板；出稿后看实际效果再定 |
